# SF_Noah Releases

本仓库**仅用于发布 SF_Noah 的更新包（GitHub Releases assets）**，不包含源码。

请到 GitHub 的 Releases 页面下载最新的 `SF_Noah-win-x64*.zip`，解压后运行 `SF_Noah.exe`。

# SF_Noah Releases

这个仓库**只用于发布二进制更新包（Release assets）**，不包含源码。

- 从 Noah 拉取缺陷详情/评论/附件（使用 TT 登录态）
- 一键下载日志压缩包、选择性导入（提取 `sf_logs.txt` / `android_log_*.txt` / 视频等）
- 打开 SF 3D 可视化窗口（WebView2）
- **每条缺陷一键生成 Cursor 分析包**：自动生成 `.code-workspace`、`.cursorrules`（WMS/窗口规则）、提示词、并尽量把日志证据一并带上
- 支持 **应用内检查更新/自动更新**（GitHub Releases）

---

## 下载

- 请到 GitHub 的 Releases 页面下载最新的 `SF_Noah-win-x64*.zip` 并解压运行 `SF_Noah.exe`

